user => auth => server-commands=> 


server-commands:    list_notes(userName)
                    about_me(notes) // UnImpersonated
                    about_user()
                    reply_to(note_id, note)
                    update_note(note_id, note)
                    new_note(note)
                    
                    auth_user(userName)
                    register_user(userName)
                    list_users()
